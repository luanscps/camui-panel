// Layout vazio — a shell visual vem do (panel)/layout.tsx pai.
// Este arquivo existe apenas para manter a estrutura de pastas explícita.
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
